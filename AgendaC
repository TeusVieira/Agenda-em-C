#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

#define TAM_NOME 100




        typedef struct agenda
    {
        char nome_contato [ TAM_NOME ];
        int telefone [ 11 ];
        char email [ 200 ];
        char endereco [300];
    } contato ;


void adicionarContatos( contato usuario );
void listarContatos( contato usuario );
void alterarContatos( contato usuario );
void excluirContatos( contato usuario );


int main( ){
 
    setlocale(LC_ALL,"");


        // CriaÃ§Ã£o do Arquivo e abertura do arquivo
        FILE *ptrArquivo = NULL;
        ptrArquivo = fopen ("DADOS-DO-REGISTRO.txt", "w");


        int opcao;
        contato usuario;


 // laÃ§o para o UsuÃ¡rio decidir o que o codigo ira executar
do{

        printf("------------------------------------------\n");
        printf("- 1. Listar Contatos:                    -\n");
        printf("- 2. Adicionar Contato:                  -\n");
        printf("- 3. Alterar Contato:                    -\n");
        printf("- 4. Excluir Contato:                    -\n");
        printf("- 0. Sair:                               -\n");
        printf("------------------------------------------\n");

        scanf("%i", &opcao);

    switch (opcao)
    {

    case 1:
        listarContatos(usuario);
        break;

    case 2:
        adicionarContatos(usuario);
        break;

    case 3:
        alterarContatos(usuario);
        break;

    case 4:
        excluirContatos(usuario);
        break;

    case 0:
        printf("------------------------------------------\n");
        printf("-SAINDO ,_,                              -\n");
        printf("------------------------------------------\n");
        break;

    default:
        printf("------------------------------------------\n");
        printf("- Essa Opção não Existe.                 -\n");
        printf("------------------------------------------\n");
        break;

    }

  

}while(opcao != 0);

    fclose (ptrArquivo);
    ptrArquivo = NULL;

    return 0;

}


void adicionarContatos( contato usuario )
{
     printf("Função não implementada. \n");
}


void listarContatos( contato usuario)
{
      setlocale(LC_ALL,"");
     printf("Função não implementada. \n");
}


void alterarContatos(contato usuario)
{
    int usuario_para_alterar;
    contato usuario_alterar;
    char confimar_acao;

     printf("Qual Contato deseja Alterar? \n");
     scanf("%i", &usuario_para_alterar);

     printf("NOME: \n");
     fflush(stdin);
     scanf("%[^\n]s", &usuario_alterar.nome_contato);
     printf("TELEFONE: \n");
     scanf("%i", &usuario_alterar.telefone);
     printf("EMAIL: \n");
     fflush(stdin);
     scanf("%[^\n]s", &usuario_alterar.email);
     printf("ENDEREÃO: \n");
     fflush(stdin);
     scanf("%[^\n]s", &usuario_alterar.endereco);

 do
 {

    printf("Deseja Confirmar ?? (S/N)");
    fflush(stdin);
    scanf("%c", &confimar_acao);
    confimar_acao = tolower(confimar_acao);

        if(confimar_acao == 's'){
            printf("DEU BOOM, MEU REI \n");
            break;
        }else{
            printf("NUMERO NÃO ALTERADO, MENO OLHA ISSO AI \n");
        }

 } while( confimar_acao != 's' || confimar_acao != 'n' ) ;
 
     

    

}

void excluirContatos(contato usuario)
{
     setlocale(LC_ALL,"");
     printf("Função não implementada. \n");
}

//Autor TeusVieira&Alioh
